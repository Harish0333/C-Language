#include<stdio.h>
#include<string.h>
int main()
{
	char a[100],b[100];
	printf("Enter the num to check it is a palindrome:");
	gets(a);
	strcpy(b,a);//copying the input string
	strrev(b);//reversing the input string
	if (strcmp==0)//comparing the input string with the reverse string
	printf("the enter num is a palindrome:");
	else 
	printf("the entered num isnt a palindrome");
}
